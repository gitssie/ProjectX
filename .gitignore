
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# ProjectX deployment config, state, repository, and logs are local-only.
.deploy/

# Local build products and platform metadata are not source artifacts.
.theos/
packages/
_deb_extract/
build_check/
/libAppDataCleaner.dylib
.DS_Store
Thumbs.db
ProjectX.entitlements.xml
ent.plist.xml
